# Инструменты разработки — сверх requirements.txt. Для работы оболочки не нужны.
#
# Playwright поднимает настоящий браузер для tools/ui_check.py: дефекты
# интерфейса этого проекта раз за разом жили в доставке результата до экрана,
# и видны они только в браузере. После установки нужен сам Chromium:
#     .venv\Scripts\python -m playwright install chromium
playwright>=1.47

# Линтер; правила — в ruff.toml. Проверка: .venv\Scripts\python -m ruff check .
ruff>=0.6
