# Настройки pytest.
#
# basetemp — внутри проекта: временные каталоги тестов (tmp_path) не должны
# уходить в системный %TEMP%. Каталог tmp/ под .gitignore, pytest очищает
# basetemp в начале каждого прогона.
[pytest]
testpaths = tests
addopts = --basetemp=tmp/pytest
