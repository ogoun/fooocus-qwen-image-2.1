# Shell-скрипты обязаны иметь перевод строки LF: при CRLF строка #! ломается
# под Git Bash и WSL, и скрипт не запускается.
*.sh text eol=lf
# PowerShell 5.1 неверно читает кириллицу в UTF-8 без BOM, поэтому .ps1
# хранятся как есть и нормализации не подлежат.
*.ps1 -text
